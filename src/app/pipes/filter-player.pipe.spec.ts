import {FilterPlayerPipe} from './filter-player.pipe';

describe('FilterPlayerPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterPlayerPipe();
        expect(pipe).toBeTruthy();
    });
});
