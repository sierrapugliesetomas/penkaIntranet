import {FilterGoalScorerPipe} from './filter-goal-scorer.pipe';

describe('FilterGoalScorerPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterGoalScorerPipe();
        expect(pipe).toBeTruthy();
    });
});
