import {FilterPodiumPipe} from './filter-podium.pipe';

describe('FilterPodiumPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterPodiumPipe();
        expect(pipe).toBeTruthy();
    });
});
