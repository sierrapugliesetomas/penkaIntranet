import {FilterCodePenkaPipe} from './filter-code-penka.pipe';

describe('FilterCodePenkaPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterCodePenkaPipe();
        expect(pipe).toBeTruthy();
    });
});
