import {FilterTeamPipe} from './filter-team.pipe';

describe('FilterTeamPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterTeamPipe();
        expect(pipe).toBeTruthy();
    });
});
