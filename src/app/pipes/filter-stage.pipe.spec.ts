import {FilterStagePipe} from './filter-stage.pipe';

describe('FilterStagePipe', () => {
    it('create an instance', () => {
        const pipe = new FilterStagePipe();
        expect(pipe).toBeTruthy();
    });
});
