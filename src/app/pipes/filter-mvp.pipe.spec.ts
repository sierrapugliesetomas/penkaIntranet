import {FilterMvpPipe} from './filter-mvp.pipe';

describe('FilterMvpPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterMvpPipe();
        expect(pipe).toBeTruthy();
    });
});
