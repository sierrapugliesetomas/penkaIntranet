import { FilterFiledPipe } from './filter-filed.pipe';

describe('FilterFiledPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFiledPipe();
    expect(pipe).toBeTruthy();
  });
});
