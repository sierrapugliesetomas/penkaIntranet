import {FilterSelectablePipe} from './filter-selectable.pipe';

describe('FilterSelectablePipe', () => {
    it('create an instance', () => {
        const pipe = new FilterSelectablePipe();
        expect(pipe).toBeTruthy();
    });
});
