import { FilterPublishPipe } from './filter-publish.pipe';

describe('FilterPublishPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPublishPipe();
    expect(pipe).toBeTruthy();
  });
});
