import { FilterCodeTemplatePipe } from './filter-code-template.pipe';

describe('FilterCodeTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCodeTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
